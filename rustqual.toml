# rustqual configuration for memx workspace
# Dead code detection off — pub API is consumed cross-crate
[duplicates]
detect_dead_code = false

[srp]
# sqlite_store.rs is a single-adapter file — 340 lines is acceptable
file_length_baseline = 400
